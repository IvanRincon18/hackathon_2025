version: 1

models:
  - name: prediction_status
    columns:
      - name: prediction_date
        tests:
          - not_null
      - name: real_case
      - name: lowest_case
      - name: greatest_case
